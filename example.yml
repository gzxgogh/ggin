app:
  name: "appName"
  listen: "0.0.0.0:8310"

logger:
  type: info,err,panic
  path: logs
  requestTableName:

mysql:
  conn: user:pwd@tcp(localhost:port)/dbname?charset=utf8&parseTime=True&loc=Local
  used: true
mongo:
  used: true
  conn: mongodb://user:pwd@localhost:port
  db: test
redis:
  used: false
  addr: local:port
  password:
  db: 0
rabbitMq:
  conn: amqp://user:pwd@localhost:port/
  used: true

